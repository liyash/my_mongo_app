<!DOCTYPE html>
<html>
<head><title>User List</title></head>
<body>
    <h1>Users</h1>
    <a href="/users/create">Create User</a>
    <ul>
        {% for user in users %}
            <li>
                <a href="/users/{{ user.id }}">{{ user.name }}</a>
                ({{ user.email }},
                Age: {{ user.age is defined ? user.age : 'N/A' }})
                <form action="/users/{{ user.id }}/delete" method="post" style="display:inline;" onsubmit="return confirm('Delete this user?');">
                    <button type="submit">Delete</button>
                </form>
            </li>
        {% endfor %}
    </ul>
</body>
</html> 